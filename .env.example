# SUPABASE CONFIGURATION
# 1. Enable Row Level Security (RLS) on all tables.
# 2. Add the URL and ANON key here. NEVER use the Service Role Key here.
REACT_APP_SUPABASE_URL=https://your-project-id.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-public-key

# RAZORPAY CONFIGURATION
# 1. Use Test Key ID for development, Live Key ID for production.
# 2. NEVER put the Key Secret here. It belongs in the Backend/Edge Function.
REACT_APP_RAZORPAY_KEY_ID=rzp_test_your_key_id

# GOOGLE GENAI CONFIGURATION
# 1. Restrict this key to your domain in Google Cloud Console.
# IMPORTANT: The code specifically looks for "API_KEY", not "REACT_APP_API_KEY"
API_KEY=your_google_gemini_api_key